SPECIFICATION Spec
\* Add statements after this line.

CONSTANTS
    num_messages = 2
    alphabet =  { "A", "B"}
    \* num_messages = 4
    \* alphabet =  { "A", "B", "C", "D"}
    buffer_size =  3
    NULL = "NULL"

PROPERTIES
Eventually_Complete

\*
\* The following property is violated:
\* Let's figure out why

ReaderWriterSpec

INVARIANTS
Reads_Accurate
